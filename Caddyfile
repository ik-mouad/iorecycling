:88 {
  encode gzip

  # Keycloak est maintenant accessible directement sur le port 8080
  # On proxy /auth/* vers keycloak:8080 en préservant le path
  reverse_proxy /auth/* keycloak:8080

  handle_path /api/* {
    reverse_proxy backend:8080
  }

  handle {
    reverse_proxy frontend:80
  }
}

