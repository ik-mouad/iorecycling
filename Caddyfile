:88 {
  encode gzip

  # Keycloak avec KC_HTTP_RELATIVE_PATH=/auth
  # On proxy /auth/* vers keycloak:8080 SANS strip du /auth
  reverse_proxy /auth/* keycloak:8080

  handle_path /api/* {
    reverse_proxy backend:8080
  }

  handle {
    reverse_proxy frontend:80
  }
}

